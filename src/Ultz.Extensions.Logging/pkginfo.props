<Project>
    <PropertyGroup>
        <Description>
            A Microsoft.Extensions.Logging.Abstractions implementation, only with more customaization and less faff.
        </Description>
        <Version>3.0.0</Version>
        <LangVersion>9</LangVersion>
        <PackageReleaseNotes>
            Removes pre-formatting of DateTime and EventId, coalescing strings 5 (day), 6 (month), 7 (year), 8 (hour),
            9 (minute), and 10 (second) into just 5 (DateTime). Also, adds WaitForIdle and WaitAndShutdown to fix issues
            where logging isn't finished by the time work is done.
        </PackageReleaseNotes>
        <GenerateDocumentationFile>true</GenerateDocumentationFile>
        <PackageLicenseExpression>MIT</PackageLicenseExpression>
        <RepositoryUrl>https://github.com/Ultz/Bcl</RepositoryUrl>
        <RepositoryType>git</RepositoryType>
    </PropertyGroup>
</Project>